# nodeLinker: node-modules

# npmAuthToken: ${GITHHUB_NPM_TOKEN}

# npmRegistries:
#   //npm.pkg.github.com/:
#     npmAlwaysAuth: true
#     npmAuthToken: ${GITHHUB_NPM_TOKEN}

npmScopes:
  # "@fitnesspassport":
  #   npmAlwaysAuth: true
  #   npmAuthToken: ${GITHHUB_NPM_TOKEN}
  #   npmPublishRegistry: "https://npm.pkg.github.com/"
  #   npmRegistryServer: "https://npm.pkg.github.com/"
  fitnesspassport:
    npmAlwaysAuth: true
    npmAuthToken: ${GITHUB_NPM_TOKEN}
    npmPublishRegistry: "https://npm.pkg.github.com/"
    npmRegistryServer: "https://npm.pkg.github.com/"
